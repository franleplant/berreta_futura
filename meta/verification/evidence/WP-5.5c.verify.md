# WP-5.5c verification

Verdict: **ACCEPTED** (one test-coverage advisory, below)

Verified at art_directed `01ca472` in a fresh worktree, own target dir.
Worker commit `b31d651`; its paths are all under `mag/src/package*`,
`mag/tests/package*` and its evidence file.

## Replay

- `cargo fmt --check` exit 0; `cargo clippy -q --all-targets -- -D warnings`
  exit 0; `cargo test` exit 0, 31 binaries, 637 passed, 0 failed.
- Spec rebuilt from `editions/010/render-2026-09-14T01-49-02/en` with the
  evidence's script into my own scratch (`tmp/v4/spec.json`). Python oracle
  regenerated by me: exit 0, 123 files, 25.9 s; its SHA256SUMS equals the
  shipped one on every line except `preflight.json` (122 lines).
- Tracer dump: `MODE: full`, 56/28/26/1 pages, sha256 `e68b23f8...bbbd1948`
  (matches the evidence). Tracer-substituted oracle differs from py-out on
  `render-critic.json` only.
- Gated run (`MAG_PACKAGE_*` set, `--release --test-threads 1`): exit 0, 51
  passed. `CLASSES {"bytes": 90, "pdf": 3, "pixels": 28, "report": 1}`,
  report vs tracer Python `["/visual_review/booklet_sha256"]`, vs pypdf
  Python 57 leaves, 56 text fields, six contact sheets 0 differing channel
  bytes. Numbers identical to the evidence.
- Skipped mode: 51 passed, prints `MODE skipped`. Only `MAG_PACKAGE_SPEC`
  set: the test fails with `set all of ... or none` (exit non-zero).

## The orchestrator's package oracle (QUEUE.md, rule 6c), checked independently

My own checker (`tmp/v4/rule.py`, Python, PIL + poppler), Rust output from
the gated run vs my py-out:

| rule | result |
|---|---|
| SHA256SUMS: same files, same order | 122 names, identical order; every Rust line is its file's real digest |
| JSON/text byte-equal | 90 entries byte-equal (all text/JSON except render-critic.json); no text entry differs |
| render-critic.json | differs; excepted per the rule (booklet_sha256) plus the recorded tracer text source |
| PNGs pixel-equal | 28 non-byte-equal PNGs: same mode (all RGB), same size, `tobytes()` equal |
| PDFs page-count and page-raster equal | booklet-a4 28 pp, interior 26 pp, cover 1 p: pdfinfo counts equal, pdftoppm at 110 dpi every page byte-equal |
| archives per entry | Python `_archive_tree(py-out)` vs Rust `archive_tree(ours)`: 123 entries, same names and order, date/attr/method equal on all, CRC/size/contents equal on every byte-equal file |

The rule holds on real 010 output.

## Injected defects (mine, not the worker's)

1. Removed `files.sort_by_key(...)` before SHA256SUMS is written (insertion
   order): the gated live test FAILS at `entry list and order`.
2. Critic bail condition `result == "fail"` changed to `"FAIL"` (the bail
   never fires): the gated failure-path test FAILS (preflight.json written).

Both restored, `git status` clean.

## Advisory (not blocking)

- Both defects above are caught ONLY in gated mode. With the env unset,
  `cargo test --test package` passes 51/51 with the sort removed: nothing in
  the default suite pins SHA256SUMS order or the critic bail. A small
  synthetic package fixture would pin them.
- The live test checks PDFs by page count only. Page-raster equality for the
  booklet and cover follows indirectly from the byte-equal render-review
  rasters in SHA256SUMS, but the interior PDF (26 pp) has no raster in the
  package; its raster equality is shown only by my checker above. Adding a
  per-page pdftoppm comparison to the PDF branch would make the test match
  the rule.

## What is and is not proven

Proven on 010: the worker's numbers reproduce from an oracle I generated;
the orchestrator's equality rule holds entry by entry, including interior
page rasters. Not proven: anything beyond 010 and the fixtures; correctness
of values beyond equality with Python (as the evidence states).
