# Cache Budgets

A paragraph of captured prose that the manuscript must not repeat verbatim.

BEGIN-CODE
budget = lambda tokens: tokens // 4
ceiling = budget(4096)
END-CODE

The quoted run starts here. It runs across a blank line.

And it ends on this line, verbatim.

A duplicated marker appears here: AMBIGUOUS
And the duplicated marker appears again: AMBIGUOUS
