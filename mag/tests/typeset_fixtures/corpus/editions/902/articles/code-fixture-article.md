---
source_ids:
- fixture-source-c
content_mode: article
label: ARTICLE
---

The standfirst paragraph opens the code fixture and names `budget()` inline.

A lead paragraph opens the body on the page after the opener.

## Background

Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. 

Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. 

Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. 

Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. 

Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. 

## Setup

A paragraph before the first fence, in the ordinary reading measure.

```rust
fn budget(tokens: u32) -> u32 {
    // four characters per token
    let spare = tokens.rem_euclid(4);
    tokens.div_ceil(spare)
}
```

## Tooling

A paragraph between the fences.

```typescript
export const ceiling = (limit: number): string =>
  `cap ${limit} tokens`;
```

```bash
echo "spent $SPENT of 4096" # report
```

A closing paragraph after the fences.

## References

- Ada Fixture. Cache Budgets in Practice. A reference entry long enough to wrap onto a second line so that its hanging indent shows on the page, and then onto a third line, so the second line runs the full indented measure.
- Bo Fixture. Storage Tiers. A second, shorter entry.

1. An ordered entry under the same heading keeps the body style.
