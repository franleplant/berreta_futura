{
  "text": "Fixture Press Issue 902 Code Fixture 2026-09-03 Issue 902 / Contents Contents Feature 01A Code Fixture ArticleCy Fixture Feature 02The Second Code ArticleDee Fixture Feature 03The Third Code ArticleDee Fixture Feature 04The Fourth Code ArticleDee Fixture Feature 01 / ARTICLE / 2026 08 A Code Fixture Article By Cy Fixture A fixture author note for the illustrated opener. https://example.invalid/c The standfirst paragraph opens the code fixture and names budget() inline. A lead paragraph opens the body on the page after the opener. Background Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Budgets start as a guess and harden into a rule once the numbers come back. A team that counts tokens by hand soon wants a helper, and a helper soon wants a test. The fixture keeps this prose plain, so the page fills with serif. Setup A paragraph before the first fence, in the ordinary reading measure. fn budget(tokens: u32) -> u32 { // four characters per token let spare = tokens.rem_euclid(4); tokens.div_ceil(spare) } Tooling EXTRACT-BEGIN let spent = budget(4096); assert!(spent <= 1024, \"over budget\"); EXTRACT-END The captured budget call. A paragraph between the fences. export const ceiling = (limit: number): string => `cap ${limit} tokens`; echo \"spent $SPENT of 4096\" # report A closing paragraph after the fences. References Ada Fixture. Cache Budgets in Practice. A reference entry long enough to wrap onto a second line so that its hanging indent shows on the page, and then onto a third line, so the second line runs the full indented measure. Bo Fixture. Storage Tiers. A second, shorter entry. An ordered entry under the same heading keeps the body style. End / 01 Feature 02 / ARTICLE / 2026 08 The Second Code Article By Dee Fixture A fixture author note for the illustrated opener. https://example.invalid/d The second article’s standfirst sits under its own plain opener code. A lead paragraph opens the body on the page after the opener. Notes A short paragraph so the second article closes on its first page. End / 02 Feature 03 / ARTICLE / 2026 08 The Third Code Article By Dee Fixture A fixture author note for the illustrated opener. https://example.invalid/d The third article’s standfirst sits under its own plain opener code. A lead paragraph opens the body on the page after the opener. Notes A short paragraph so the third article closes on its first page. End / 03 Feature 04 / ARTICLE / 2026 08 The Fourth Code Article By Dee Fixture A fixture author note for the illustrated opener. https://example.invalid/d The fourth article’s standfirst sits under its own plain opener code. A lead paragraph opens the body on the page after the opener. Notes A short paragraph so the fourth article closes on its first page. End / 04",
  "verbatim": [
    "fn budget(tokens: u32) -> u32 {\n    // four characters per token\n    let spare = tokens.rem_euclid(4);\n    tokens.div_ceil(spare)\n}",
    "EXTRACT-BEGIN\nlet spent = budget(4096);\nassert!(spent <= 1024, \"over budget\");\nEXTRACT-END",
    "export const ceiling = (limit: number): string =>\n  `cap ${limit} tokens`;",
    "echo \"spent $SPENT of 4096\" # report"
  ]
}
