source: https://arxiv.org/pdf/1810.04805v2
retrieved: "2026-09-23"
sha256: 5692a5514787a8c6727b4ff3b726a3385798bc68e12138d1d4af83947e2acf6e
producer: "pdfTeX-1.40.17, 16 pages, Type1 fonts plus TrueType Identity-H, ligatures fi/fl/ffi in the text layer"
shape: a two-column conference paper (ACL format), with a full-width figure caption spanning both columns and footnotes at the foot of a column
line_end_hyphens:
  - "representa-|tion model"
  - "apply-|ing pre-trained"
  - "representations to down-|stream tasks:"
  - "only at-|tend to"
  - "the segmenta-|tion embeddings"
real_hyphens:
  - "can be fine-|tuned with"
  - "substantial task-|specific architecture"
  - "use a left-to-|right architecture"
  - "related to representation-|learning objectives"
  - "since the self-|attention mechanism"
  - "state-of-the-art models"
  - "pre-trained BERT model"
  - "feature-based and"
  - "question-passage pairs"
absent:
  - "finetuned"
  - "left-toright"
order:
  - - "We introduce a new language"
    - "De Meulder, 2003; Rajpurkar"
    - "There are two existing strategies"
    - "the original vocabulary id of the masked"
  - - "Figure 2: BERT input representation."
    - "The NSP task is closely related"
    - "pairs in question answering, and"
    - "(4) a degenerate text"
    - "The General Language Understanding Evaluation"
    - "around 30 minutes on a single Cloud TPU"
review:
  - at: "p1 abstract, can be fine-|tuned"
    pypdf: "\\ufb01ne-\\ntuned, hyphen kept but the fi ligature emitted as U+FB01"
    poppler: "finetuned, fi decoded but the hyphen deleted"
    truth: "fine-tuned: the page prints fine- at the line end; the compound is the paper's term (fine-tuning appears in-line throughout); both extractors corrupt the word, differently"
  - at: "p1 right column, use a left-to-|right architecture"
    pypdf: "left-to-\\nright"
    poppler: "left-toright"
    truth: "left-to-right, a compound whose second hyphen fell at the line end; welding it is the plan's named defect"
  - at: "p1, the rotated arXiv stamp arXiv:1810.04805v2 [cs.CL] 24 May 2019 in the left margin"
    pypdf: "emitted after the page's body text, with doubled spaces"
    poppler: "emitted between the author block and the abstract"
    truth: "a repository stamp, not the article's text; no check constrains it, and neither placement is counted as an error"
  - at: "p1, column order: the last line of the left column (Rajpurkar et al., 2016). then There are two existing strategies at the top of the right column"
    pypdf: "left column then right column"
    poppler: "left column then right column"
    truth: "agreement; the order passages pin it; poppler -layout interleaves the two columns line by line and fails every passage that spans a line"
  - at: "p5, (3) question-passage pairs ... and (4) a degenerate text-∅ pair, which runs from the foot of the left column to the head of the right column"
    pypdf: "column order right; text- ∅ with a space, and classi\\ufb01cation with the ligature codepoint"
    poppler: "text-∅ and classification, as printed"
    truth: "text-∅ with no space; the passage checks the column crossing and the ligature together"
  - at: "p5 left column, the word tasks and an em dash end a line, whether starts the next"
    pypdf: "tasks—\\nwhether"
    poppler: "tasks—\\nwhether"
    truth: "agreement; the normaliser treats whitespace after a word-final dash as the line break it is, so this is not scored"
