source: https://arxiv.org/pdf/1912.01703v1
retrieved: "2026-09-23"
sha256: b47fb010f6872701d6d3bdd8270497be700c9961e2ec2cac2b08a154a43d81f2
producer: "pdfTeX-1.40.17, 12 pages, Type1 fonts, syntax-highlighted monospace listings"
shape: a single-column paper with code blocks, one listing set as two side-by-side code columns, and a 3-column author grid
line_end_hyphens:
  - "the length op-|erator"
  - "improves the perfor-|mance of"
  - "by com-|paring it"
real_hyphens:
  - "formally the vector-|Jacobian product"
  - "executed using forward-|mode automatic"
  - "a common anti-|pattern among"
  - "The one-pool-per-stream design"
  - "single-machine eager mode"
absent:
  - "class linearlayer"
  - "class fullBasicModel"
order:
  - - "adam.paszke@gmail.com"
    - "Andreas Köpf"
    - "Deep learning frameworks have often focused"
    - "speed of PyTorch on several common benchmarks"
    - "Note that linear layers are of course"
  - - "def __init__(self, in_sz, out_sz):"
    - "return t + self.b"
    - "self.conv = nn.Conv2d(1, 128, 3)"
    - "return nn.functional.softmax(t3)"
    - "Listing 1: A custom layer"
  - - "in PyTorch easily adapts to this setting"
    - "discriminator = create_discriminator()"
    - "errG.backward()"
    - "Listing 2:"
    - "Since PyTorch programs execute eagerly"
    - "interoperability is one of the top priorities"
review:
  - at: "p4 Listing 1, class LinearLayer(Module): and class FullBasicModel(nn.Module):"
    pypdf: "class linearlayer (Module): class fullBasicModel (nn.Module): on one line, the two code columns interleaved line by line, the bold class names case-folded and a space inserted before the parenthesis"
    poppler: "the left column's first seven lines, then the whole right column, then the left column's def forward(self, activations) block after it"
    truth: "two separate code blocks, left then right, each with 3-space indentation; the page shows LinearLayer and FullBasicModel in bold with capitals. pypdf corrupts identifiers, poppler moves a method out of its class; both fail, differently"
  - at: "p4 Listing 2, def step(real_sample): and its body"
    pypdf: "lines in order, all indentation lost"
    poppler: "lines in order, all indentation lost"
    truth: "agreement on the text, and agreement on a defect: the page indents the body by 2 spaces, so code:2 passes both and code-exact:2 fails both; poppler -layout keeps the relative indentation"
  - at: "p4 Listing 2 line 10, errD_fake = loss(discriminator(fake.detach(), fake_label)"
    pypdf: "as printed, parentheses unbalanced"
    poppler: "as printed, parentheses unbalanced"
    truth: "the listing itself is missing a parenthesis; verbatim means the defect is kept"
  - at: "p1 author grid, three columns of name / affiliation / email blocks"
    pypdf: "block by block, row-major"
    poppler: "block by block, row-major"
    truth: "agreement; each block keeps name, affiliation and email together (Andreas Köpf Xamla andreas.koepf@xamla.com); poppler -layout sets the grid row by row and splits every block"
  - at: "p5, formally the vector-|Jacobian product"
    pypdf: "vector-\\nJacobian"
    poppler: "vectorJacobian"
    truth: "vector-Jacobian product, a compound; pypdf and poppler -layout agree on the hyphen at the line end"
  - at: "p1 abstract, Pythonic programming style ... scientific computing libraries, while remaining efficient"
    pypdf: "scientiﬁc and efﬁcient with U+FB01"
    poppler: "scientific and efficient"
    truth: "the letters f and i; the ligature is a typesetting glyph, not the author's text"
