#!/bin/sh
set -eu
cd "$(git rev-parse --show-toplevel)"
(cd mag && cargo fmt --check && cargo clippy -q --all-targets -- -D warnings)
uvx ruff format --check src/magazine tools
uvx ruff check src/magazine tools
python3 tools/nocomments.py
(cd mag && cargo test -q --test nocomments)
